# اسناد فارسی قاصدک

این پوشه دقیقاً شامل ۸ سند HTML مستقل، RTL، دارای فهرست مطالب، استایل یکدست و CSS مناسب چاپ/A4 است. فونت ترجیحی در CSS `Vazirmatn` تعریف شده و در نبود آن از fallback سیستم استفاده می‌شود؛ فایل فونت در مخزن بسته‌بندی نشده است.
